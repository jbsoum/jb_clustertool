apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease

metadata:
  name: dev-jbsoum-org
  namespace: dev-jbsoum-org

spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 15.29.82
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
  #timeout: 20m
  maxHistory: 3
  driftDetection:
    mode: warn
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false

  values:
    global:
      stopAll: false

    image:
      repository: docker.io/bitnami/postgresql
      pullPolicy: IfNotPresent
      tag: latest@sha256:8c7f26a8ba4257fcbf4445bd50e156cbc7d8114942fb3d6523320c7732781b54

    TZ: ${TIMEZONE}
    credentials:
      backblaze:
        accessKey: ${S3_ACCESS_KEY}
        bucket: ${S3_BUCKET}
        encrKey: ${S3_ENCRYPTION_KEY}
        name: ${S3_NAME}
        path: ""
        secretKey: ${S3_SECRET_KEY}
        type: s3
        url: ${S3_URL}

    securityContext:
      container:
        runAsGroup: 0
        readOnlyRootFilesystem: false

    service:
      main:
        ports:
          main:
            port: ${PORT_DEV_JBSOUM_ORG}
            targetPort: 5432
        enabled: true
        type: LoadBalancer
        integrations:
          metallb:
            enabled: true

        loadBalancerIP: ${IP_DEV_JBSOUM_ORG}

    persistence:
      data:
        enabled: true
        type: emptyDir
        mountPath: "/bitnami/postgresql"

    workload:
      main:
        podSpec:
          containers:
            main:
              env:
                POSTGRESQL_USERNAME: ${DEV_JBSOUM_ORG_USERNAME}
                POSTGRESQL_PASSWORD: ${DEV_JBSOUM_ORG_PASSWORD}
                POSTGRESQL_DATABASE: ${DEV_JBSOUM_ORG_USERNAME}
                POSTGRESQL_MASTER_PORT_NUMBER: ${PORT_DEV_JBSOUM_ORG}
                POSTGRESQL_TIMEZONE: ${TIMEZONE}
                POSTGRESQL_LOG_TIMEZONE: ${TIMEZONE}
