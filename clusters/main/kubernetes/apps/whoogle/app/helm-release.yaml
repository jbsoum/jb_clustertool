apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
    name: whoogle
    namespace: whoogle
spec:
    interval: 15m
    chart:
        spec:
            chart: whoogle
            version: 11.0.8
            sourceRef:
                kind: HelmRepository
                name: truecharts
                namespace: flux-system
    releaseName: whoogle
    values:
        TZ: ${TIMEZONE}
        credentials:
            backblaze:
                accessKey: ${S3_ACCESS_KEY}
                bucket: ${S3_BUCKET}
                encrKey: ${S3_ENCRYPTION_KEY}
                name: ${S3_NAME}
                path: ""
                secretKey: ${S3_SECRET_KEY}
                type: s3
                url: ${S3_URL}
        ingress:
            main:
                enabled: true
                hosts:
                    - host: ${DOMAIN_WHOOGLE}
                      paths:
                        - path: /
                          pathType: Prefix
                tls:
                    - certificateIssuer: ""
                      clusterCertificate: ${DNS_CLUSTER_CERT_NAME}
                      hosts:
                        - "{{ .Values.ingress.main.hosts.host }}"
                      secretName: ""
                integrations:
                    homepage:
                        enabled: true
                        group: Utilities
                        widget:
                            enabled: false
                    certManager:
                        enabled: false
                        certificateIssuer: ""
                    traefik:
                        enabled: true
                        entrypoints:
                            - websecure
                        middlewares:
                            - name: local
                              namespace: traefik
                            - name: ${TRAEFIK_MIDDLEWARE}
                              namespace: traefik
        persistence:
            config:
                volsync:
                    - credentials: ${S3_NAME}
                      dest:
                        enabled: true
                      name: config
                      src:
                        enabled: true
                      type: restic
        release_name: whoogle
        service:
            main:
                enabled: true
                ports:
                    main:
                        port: 10052
                type: ClusterIP
        workload:
            main:
                podSpec:
                    containers:
                        main:
                            env:
                                WHOOGLE_ALT_RD:
                                    name: WHOOGLE_ALT_RD
                                    value: ${DOMAIN_LIBREDDIT}
                                WHOOGLE_ALT_YT:
                                    name: WHOOGLE_ALT_YT
                                    value: ${DOMAIN_INVIDIOUS}
                                WHOOGLE_CONFIG_ALTS: true
                                WHOOGLE_CONFIG_COUNTRY: US
                                WHOOGLE_CONFIG_GET_ONLY: false
                                WHOOGLE_CONFIG_NEW_TAB: true
                                WHOOGLE_CONFIG_SAFE: true
                                WHOOGLE_CONFIG_STYLE: ""
                                WHOOGLE_CONFIG_THEME: dark
                                WHOOGLE_CONFIG_TOR: false
                                WHOOGLE_CONFIG_URL: https://${DOMAIN_WHOOGLE}
